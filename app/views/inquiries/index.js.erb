$.when($(".paginator").remove()).then(function(){
  var partial = $('<%= j render "inquiries" %>');
  var lis = partial.find('li.paginator, li.inquiry, li.inquiry-details');
  //.hide();
  $("#live-inquiries ul").append(lis);
  // lis.fadeIn();
  //$(lis).find('li.paginator, li.inquiry').show()
});

